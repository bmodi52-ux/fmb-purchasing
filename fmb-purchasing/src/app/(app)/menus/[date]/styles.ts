/**
 * Taking a line off the day (#21). It was a faint lowercase link, easy to miss
 * next to the counts, so it is a button that looks like one.
 */
export const REMOVE_BUTTON =
  "btn btn-danger btn-xs";
